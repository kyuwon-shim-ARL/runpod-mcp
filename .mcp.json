{
  "mcpServers": {
    "runpod": {
      "command": "node",
      "args": ["/home/kyuwon/projects/runpod-mcp/dist/runpod-mcp.mjs"],
      "env": {
        "RUNPOD_API_KEY": "${RUNPOD_API_KEY}",
        "SSH_KEY_PATH": "${SSH_KEY_PATH}"
      }
    }
  }
}
