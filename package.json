{
  "name": "runpod-tools-mcp",
  "version": "0.1.0",
  "description": "MCP server for RunPod GPU pod management - full lifecycle, SSH, file transfer, GPU stock checking",
  "keywords": ["mcp", "runpod", "gpu", "claude-code", "ai"],
  "license": "MIT",
  "author": "Kyuwon Shim",
  "type": "module",
  "bin": {
    "runpod-tools-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
